apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
  namespace: immich
spec:
  interval: 5m
  chart:
    spec:
      chart: immich
      version: 0.9.3
      sourceRef:
        kind: HelmRepository
        name: immich-charts
        namespace: immich
  values:
    image:
      tag: v1.132.3
    immich:
      persistence:
        library:
          enabled: true
          existingClaim: local-retain
      server:
        metrics:
          enabled: true
    postgres:
      enabled: true
    redis:
      enabled: true
    typesense:
      enabled: true